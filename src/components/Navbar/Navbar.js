import React from "react";

import "./navbar.css";

const Navbar = () => {
    return (
        <nav className="navbar">
            <span className="navbar-brand">fampay</span>
        </nav>
    );
};

export default Navbar;
